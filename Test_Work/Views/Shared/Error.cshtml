@model List<string>;

@{ ViewData["Title"] = "Edit"; }

<h3>Application Error</h3>

<hr />
<div class="row">
    <div class="container">
        @{ 
            foreach (var error in Model)
            {
                {<p>@error </p>}
            }
         }
    </div>
</div>

<hr />

<div>
    <a asp-action="Index">Back to List</a>
</div>